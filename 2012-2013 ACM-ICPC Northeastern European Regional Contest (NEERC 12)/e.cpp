#include <cstdio>
#include <algorithm>
#include <iostream>
#include <set>
#include <iomanip>
#include <vector>
#include <queue>
#include <bitset>
#include <map>

using namespace std;
int main(){
	ios_base::sync_with_stdio(0);
	freopen("a.in", "r", stdin);
	//freopen("exact.out", "w", stdout);
	long long x, k[100100], q[100100];
	int n;
	cin >> x >> n;
	for (int i = 0; i < n; i++){
		cin >> k[i] >> q[i];
	}
	return 0;
}