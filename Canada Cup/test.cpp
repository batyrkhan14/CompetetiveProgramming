#include <cstdio>
#include <algorithm>
#include <iostream>
#include <set>
#include <iomanip>
#include <vector>
#include <queue>
#include <map>
#define INF 100000000
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    freopen("a.in","r",stdin);
    int c, n, x[200100], dp[200100];
    set<int> s;
    s.insert(1);
    s.insert(2);
    s.insert(3);
    co;
    return 0;
}