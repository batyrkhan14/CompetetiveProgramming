#include <cstdio>
#include <algorithm>
#include <iostream>
#include <set>
#include <iomanip>
#include <vector>
#include <queue>
#include <map>
#define INF 100000000
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    freopen("a.in","r",stdin);
    int c, n, x[200100];
    bool dp[200100];
    set<int> s;
    cin >> c >> n;
    for (int i = 0; i < n; i++){
        cin >> x[i];
        s.insert(s);
    }
    sort(x, x+n);
    dp[0] = true;
    for (int i = 0; i <= c; i++){
        
    }
    return 0;
}